const $ = require('jquery');

module.exports = function(elem) {
  $(document).ready(() => {
   
    
  })

  // var flag = true;
  // $('.slider__controls-item--prev, .slider__controls-item--next').on('click', function(e) {
    
  //   var animationState = $.Deferred();
  //     if (flag) {
  //       flag = false;
  
  //       var letter = block.find('.letter-span'),
  //         counter = 0,
  //         timer,
  //         duration = 1000 / string.split('').length;
  
  //       function showLetters() {
  //         var currenLetter = letter.eq(counter);
  
  //         currenLetter.addClass('active');
  
  //         counter++;
  
  //         if (string.split('').length == counter) {
  //           animationState.resolve();
  //         }
  
  //         if (typeof timer != 'undefined') {
  //           clearTimeout(timer);
  //         }
  
  //         timer = setTimeout(showLetters, duration);
  //       }
  
  //       showLetters();
  //     }
  
  //     animationState.done(function () {
  //       flag = true;
  //     });
    
  // })
}